'use strict';

/**
 * tool-currency-exchange-rate service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tool-currency-exchange-rate.tool-currency-exchange-rate');

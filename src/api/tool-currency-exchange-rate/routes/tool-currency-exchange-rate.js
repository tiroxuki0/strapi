'use strict';

/**
 * tool-currency-exchange-rate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tool-currency-exchange-rate.tool-currency-exchange-rate');

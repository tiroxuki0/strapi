'use strict';

/**
 * tool-gold-price controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tool-gold-price.tool-gold-price');
